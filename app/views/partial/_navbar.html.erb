<nav class="navbar navbar-expand-lg navbar-light">
  <div class="container ">
    <%= link_to "", root_path, class: "navbar__brand navbar__mainLogo" %>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav ml-md-auto align-items-center">
        <% if logged_in? %>
          <li>
            <%= link_to "Photo", posts_path, class: "nav-link" %>
          </li>
        <% end %>
        <li>
          <%= link_to "Bird Book", book_path, class: "nav-link" %>
        </li>
        <% if logged_in? %>
          <li>
            <%= link_to "Log Out", logout_path, class: "nav-link", method: :delete %>
          </li>
        <% else %>
          <li>
            <%= link_to "Log In", login_path, class: "nav-link" %>
          </li>
          <li>
            <%= link_to "Sign Up", new_user_path, class: "nav-link" %>
          </li>
        <% end %>
        <% if logged_in? %>
          <li>
            <%= link_to "", user_path(current_user), class: "nav-link commonNavIcon profile-icon" %>
          </li>
          <li>
            <%= "(Admin)" if current_user.admin? %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>

